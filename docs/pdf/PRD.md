# PDF 도메인 PRD
# ModuMark - PDF 처리 제품 요구사항

| 항목 | 내용 |
|------|------|
| 문서 버전 | v2.0 |
| 작성일 | 2026-03-07 |
| 상위 문서 | [docs/PRD.md](../PRD.md) · [docs/pdf/BRD.md](./BRD.md) |
| 상태 | Active (Phase 1 완료) |

---

## 목차

1. [기술 선정](#1-기술-선정)
2. [기능 요구사항 (MoSCoW)](#2-기능-요구사항-moscow)
3. [사용자 스토리](#3-사용자-스토리)
4. [처리 흐름 상세](#4-처리-흐름-상세)
5. [비기능 요구사항](#5-비기능-요구사항)
6. [UI/UX 요구사항](#6-uiux-요구사항)

---

## 1. 기술 선정

### 1.1 pdf-lib (PDF 생성·편집)

**선택 근거**:
- 순수 TypeScript/JavaScript 구현. 서버 의존 없이 브라우저에서 완전히 동작
- 보안 원칙(로컬 처리, 파일 서버 전송 없음) 완벽 충족
- PDF 병합, 분할, 생성, 페이지 조작 등 핵심 기능 지원
- 활발한 오픈소스 유지보수 (MIT 라이선스)

**한계**: 복잡한 PDF 레이아웃(CSS 기반 스타일 변환)은 제한적. 마크다운 → PDF 변환 시 스타일 제어에 추가 작업 필요.

### 1.2 PDF.js (PDF 뷰어)

**선택 근거**:
- Mozilla 재단 공식 오픈소스 PDF 렌더링 엔진
- 브라우저 내 PDF 렌더링의 사실상 표준
- Canvas 기반 렌더링으로 높은 호환성
- 페이지 이동, 줌, 검색 등 뷰어 기능 내장
- 웹 환경에서 검증된 안정성

### 1.3 Tesseract.js (OCR)

**선택 근거**:
- Tesseract OCR 엔진의 WebAssembly 포팅. 브라우저에서 직접 실행
- **서버 전송 없이 클라이언트에서 OCR 수행** → 기업 보안 정책 준수 핵심
- 한국어(`kor`) 언어팩 지원. 한국어 OCR 사용 가능
- 오픈소스 (Apache 2.0)

**한계**: 서버 사이드 Tesseract 대비 초기화 시간이 길고(언어팩 다운로드), 처리 속도가 느릴 수 있음. 초기 로딩 시 한국어 언어팩 캐싱 필요.

**검증 필요 사항**: 한국어 표준 문서 OCR 정확도 80% 이상 달성 여부를 출시 전 테스트.

---

## 2. 기능 요구사항 (MoSCoW)

### Must Have

| 기능 ID | 기능 | 설명 |
|---------|------|------|
| PDF-M1 | PDF 뷰어 | PDF.js 기반. 페이지 이동, 줌인/줌아웃, 페이지 번호 표시 |
| PDF-M2 | 마크다운 → PDF 변환 | 현재 편집 중인 마크다운 문서를 PDF로 변환·다운로드. 헤딩·코드블록·표 스타일 포함 |
| PDF-M3 | PDF 병합 | 여러 PDF 파일을 하나로 합치기. 최대 20개, 합산 100MB 이하 |
| PDF-M4 | PDF 분할 | 지정한 페이지 범위를 추출하여 새 PDF 파일 생성 |
| PDF-M5 | 처리 진행률 표시 | 병합·분할·OCR 작업 진행 상태 프로그레스 바 표시 |

### Should Have

| 기능 ID | 기능 | 설명 |
|---------|------|------|
| PDF-S1 | PDF OCR | Tesseract.js 기반. 스캔·이미지 PDF에서 텍스트 추출. 한국어 언어팩 포함 |
| PDF-S2 | OCR 신뢰도 경고 | OCR 결과 신뢰도 80% 미만 시 LOW_CONFIDENCE 경고 표시 |
| PDF-S3 | 파일 크기 제한 안내 | 단일 파일 처리 한계 초과 시 명확한 안내 메시지 |
| PDF-S4 | PDF 압축 (Compress PDF) | pdf-lib 이미지 품질 저하 압축 + 폰트 서브셋 적용으로 파일 크기 축소. 압축 레벨 선택 UI 제공. 모든 처리 클라이언트에서 수행. PROPOSAL-002 채택, Phase 2 |

### Could Have

| 기능 ID | 기능 | 설명 |
|---------|------|------|
| PDF-C1 | PDF → 마크다운 변환 | PDF 텍스트 레이어를 마크다운으로 변환. Phase 2 검토 |
| PDF-C2 | PDF 페이지 미리보기 | 병합·분할 전 페이지 썸네일로 선택 — **✅ Phase 1 구현 완료** (`PdfThumbnailList.tsx`, dnd-kit 드래그 정렬, 다중 선택, Undo 30단계 포함) |
| PDF-C3 | PDF 암호 설정·해제 | Phase 2 이후 검토 |

### Won't Have

| 기능 | 제외 이유 |
|------|----------|
| PDF 직접 텍스트 편집 | 구현 복잡도 과다, PDF 구조 특성상 어려움 |
| PDF 전자서명 | 법적 요구사항 복잡 |
| 서버 사이드 OCR | 로컬 우선 원칙, 보안 정책 위반 소지 |
| **PDF to Word 변환** | PROPOSAL-003 기각. 정확한 레이아웃 재현이 기술적으로 어려움. PDF 구조 특성상 DOCX로의 완전한 변환 불가 |

---

## 3. 사용자 스토리

### 이수진 (학생)

| ID | 스토리 | 수용 기준 |
|----|--------|----------|
| US-PDF-01 | 나는 작성한 마크다운 보고서를 버튼 하나로 PDF로 변환하여 교수님께 제출하고 싶다 | "PDF로 내보내기" 버튼 클릭 → 다운로드 완료까지 10초 이내. 헤딩·표·코드블록 스타일 포함 |
| US-PDF-02 | 나는 스캔한 강의 PDF에서 텍스트를 추출하여 마크다운으로 정리하고 싶다 | OCR 버튼 클릭 → 텍스트 추출 → 에디터 탭으로 결과 전달. 한국어 텍스트 80% 이상 인식 |

### 박지현 (사무직)

| ID | 스토리 | 수용 기준 |
|----|--------|----------|
| US-PDF-03 | 나는 여러 보고서 PDF를 하나로 합쳐서 제출하고 싶다 | PDF 파일 여러 개 선택 → 순서 지정 → 병합 → 다운로드. 최대 20개 지원 |
| US-PDF-04 | 나는 100페이지 계약서 PDF에서 특정 페이지만 분리하고 싶다 | 페이지 범위 입력(예: 3-7) → 분할 → 다운로드 |

### 최성호 (보안 민감 기업 사용자)

| ID | 스토리 | 수용 기준 |
|----|--------|----------|
| US-PDF-05 | 나는 기밀 계약서 PDF를 처리할 때 외부 서버에 파일이 업로드되지 않는다는 것을 보장받고 싶다 | 모든 PDF 처리(병합, 분할, OCR)가 브라우저/앱 내에서 수행됨. 처리 중 네트워크 요청에 파일 데이터 포함 없음. 기능 설명 페이지에 "로컬 처리" 명시 |

---

## 4. 처리 흐름 상세

### 4.1 마크다운 → PDF 변환 흐름

```
[에디터] → PDF 내보내기 버튼 클릭
    ↓
마크다운 AST를 HTML로 변환 (unified/remark)
    ↓
HTML + CSS 스타일 적용 (코드 하이라이팅, 표 스타일)
    ↓
pdf-lib로 PDF 문서 생성 (클라이언트에서 실행)
    ↓
Blob URL → 브라우저 다운로드 / Tauri FS 저장
```

### 4.2 PDF 병합 흐름

```
[사용자] → PDF 파일 여러 개 선택 (드래그 앤 드롭 또는 파일 선택)
    ↓
파일 순서 지정 (드래그 재정렬)
    ↓
pdf-lib: 각 PDF의 페이지를 새 PDFDocument에 복사 (클라이언트)
    ↓
진행률 표시 (페이지 단위)
    ↓
완료 → 다운로드
```

### 4.3 OCR 흐름

```
[사용자] → PDF 파일 선택 → OCR 시작
    ↓
PDF.js로 각 페이지를 Canvas로 렌더링
    ↓
Canvas를 이미지로 변환
    ↓
Tesseract.js Worker: 이미지 → 텍스트 (브라우저 내, 서버 전송 없음)
    ↓
신뢰도(confidence) 확인 → 80% 미만 시 LOW_CONFIDENCE 표시
    ↓
추출된 텍스트 → 새 에디터 탭으로 전달 (마크다운 형식)
```

---

## 5. 비기능 요구사항

### 5.1 성능

| 요구사항 | 목표 |
|---------|------|
| PDF 처리 완료 시간 (10MB 이하) | P90 기준 10초 이내 |
| MD → PDF 변환 시간 | 일반 문서(10페이지 이하) 3초 이내 |
| OCR 초기화 시간 (언어팩 로딩) | 최초 1회. 이후 캐싱으로 즉시 사용 |

### 5.2 보안 (핵심)

| 요구사항 | 구현 |
|---------|------|
| **파일 서버 전송 없음** | pdf-lib, PDF.js, Tesseract.js 모두 클라이언트에서 동작. 파일 Blob이 네트워크를 통해 외부로 전송되지 않음 |
| 메모리 보안 | 처리 완료된 파일 Blob은 사용 후 메모리에서 해제 (URL.revokeObjectURL) |
| 파일 크기 제한 | 단일 파일 최대 처리 크기를 안내하여 메모리 초과 방지 |

### 5.3 신뢰성

| 요구사항 | 목표 |
|---------|------|
| MD→PDF 변환 성공률 | 99% 이상 |
| PDF 병합 성공률 | 99% 이상 |
| 한국어 OCR 정확도 | 표준 문서 기준 80% 이상 |
| 실패 시 오류 메시지 | 구체적이고 이해하기 쉬운 오류 안내 |

---

## 6. UI/UX 요구사항

### 6.1 PDF 기능 진입점

- 편집기 상태 바 또는 메뉴: "PDF로 내보내기" 버튼 (현재 문서 → PDF)
- **`/pdf` 통합 에디터**: `PdfEditor.tsx` 단일 컴포넌트에서 병합·분할·뷰어 통합 제공 (Phase 1 구현). `/pdf/merge`, `/pdf/split` 하위 라우트도 병존

### 6.1.1 페이지 단위 관리 모델 (Phase 1 구현)

- **썸네일 선택**: `PdfThumbnailList.tsx`에서 PDF 파일의 각 페이지를 썸네일로 표시. 개별 또는 다중 선택 가능
- **dnd-kit 정렬**: 썸네일을 드래그하여 병합 순서 또는 페이지 순서 재배치 (`@dnd-kit/sortable`)
- **다중 페이지 선택**: 체크박스 또는 Shift+클릭으로 여러 페이지 동시 선택 후 추출·삭제
- **Undo 30단계**: `pdfFileStore.ts`의 히스토리 관리로 최대 30단계 실행 취소 지원 (`canUndo`, `undo()`)

### 6.2 처리 중 UX

- 처리 중 프로그레스 바 + 현재 처리 단계 텍스트 표시 ("페이지 3/10 처리 중...")
- 처리 중 취소 버튼 제공
- 처리 완료 후 즉시 다운로드 시작 또는 저장 완료 알림

### 6.3 오류 처리 UX

- 파일 크기 초과: "파일 크기가 100MB를 초과합니다. 분할하여 처리해 주세요."
- OCR 저신뢰도: "일부 텍스트의 인식 정확도가 낮습니다. 결과를 확인하고 수정해 주세요."
- 지원하지 않는 PDF: "이 PDF는 처리할 수 없습니다. (암호화된 PDF 등)"

---

## 문서 이력

| 버전 | 날짜 | 변경 내용 | 작성자 |
|------|------|----------|--------|
| v1.0 | 2026-03-07 | 초안 작성 | 프로젝트 오너 |
| v1.1 | 2026-03-07 | PROPOSAL-002 채택: PDF-S4 (PDF 압축) Should Have 추가. PROPOSAL-003 기각: PDF to Word Won't Have 추가 | 프로젝트 오너 |
| v2.0 | 2026-03-08 | Phase 1 완료 반영: PDF-C2 (썸네일) Phase 1 구현 완료 표시. /pdf 통합 에디터 라우트 반영. 페이지 단위 관리 모델(썸네일·dnd-kit·다중선택·Undo) 섹션 추가. 상태 Active로 변경 | 프로젝트 오너 |
